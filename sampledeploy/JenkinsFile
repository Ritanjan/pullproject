pipeline {
   agent any
   stages {
      stage('Build') {
         steps {
            withMaven(maven:'maven'){
                sh 'mvn -f sampledeploy/pom.xml clean'
            }
         }
		 }
     stage('Deploy') {
         steps {
            withMaven(maven:'maven'){
                 sh 'mvn -f sampledeploy/pom.xml clean'
            }
         }
      }
   }
}